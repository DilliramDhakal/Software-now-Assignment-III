explanations = {
    "inheritance": "We created a BaseModel class and inherited it in SentimentAnalysisModel and TextToImageModel.",
    "encapsulation": "Model pipelines are stored privately in each class and only accessed via methods.",
    "polymorphism": "Both classes implement the same run() method but behave differently depending on input.",
    "overriding": "Subclasses override load_model() and run() from the base class to provide specific functionality.",
    "decorators": "We use a custom decorator to log model execution before running."
}
